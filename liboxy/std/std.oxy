// The std.oxy is the main stdlib module. It re-exports other stdlib modules.

/* 
USAGE:

import "std" as std;
*/

@extern @symbol("puts") fn puts(str: ptr<u8>);

@public
fn println(str: ptr<u8>){
    puts(str);
}