@extern @symbol("printf") fn print(format: ptr<u8>, ...) -> i32;

@entry
fn main(argc: i32, argv: ptr<ptr<u8>>) -> u8 {
    for (let i = 0; i < argc; i++) {
        let arg = argv[i];
        print("Argument %d: %s\n", i, arg);
    }

    return 0u8;
}